.DS_Store
Thumbs.db
*.log
node_modules/
.env
.env.*
open-portal-redesign/reference/console-dev/
open-portal-redesign/reference/console-admin/

# Local tooling and deploy state, never committed
.claude/
.vercel/

# Untracked duplicate full-tree copies from an earlier copy operation.
# Ignored so a `git add -A` cannot sweep them in as embedded repos; they are
# still on disk and still awaiting Crystal's delete decision.
/about/
/advisory-implementation/
/agreeease/
/atlas/
/corporate-booking-tool/
/whitelabel/

# Python bytecode
__pycache__/
*.pyc
